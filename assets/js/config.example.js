/* ==========================================================================
   CONFIG.EXAMPLE.JS — mẫu cấu hình
   Copy file này thành  assets/js/config.js  rồi điền access key thật vào.
   config.js đã nằm trong .gitignore nên KHÔNG bao giờ bị commit lên repo.
   ========================================================================== */
window.TP_CONFIG = {
  // Lấy miễn phí tại https://web3forms.com — nhập email nhận thư, họ gửi key về.
  web3formsKey: 'DAN-ACCESS-KEY-VAO-DAY'
};
